# Copy this file to .env and fill in your own values.
# .env is git-ignored, so your real key never gets committed.

# Base URL of your Dify instance's API (note the /v1 suffix).
# Local Dify default: http://localhost/v1
DIFY_API_BASE=http://localhost/v1

# API key of your Dify *Workflow* app (starts with "app-").
# Create it in the Dify app under "API Access".
DIFY_WORKFLOW_API_KEY=app-your-workflow-api-key-here
